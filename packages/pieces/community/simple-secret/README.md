# pieces-simple-secret

Simple piece that can store your secrets as connection.

## Building

This library was generated with [Nx](https://nx.dev).

Run `nx build pieces-simple-secret` to build the library.
